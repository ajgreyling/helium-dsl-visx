{
  "name": "helium-dsl-visx",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Helium Rapid DSL (ANTLR4) VSCode Extension - Monorepo",
  "workspaces": [
    "helium-dsl-language-server",
    "helium-rapid-dsl-mcp",
    "helium-vscode-tooling",
    "helium-dsl-vscode"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "npm run clean --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
